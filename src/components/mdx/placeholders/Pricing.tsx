import React from 'react'

/** T12 placeholder — pricing table */
export function Pricing(_props: { [k: string]: unknown }) {
  return (
    <div data-lbus-component="pricing" style={{ display: 'none' }} aria-hidden="true">
      {/* Pricing — implemented in T12 */}
    </div>
  )
}
